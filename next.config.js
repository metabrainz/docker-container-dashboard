/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: '/docker-container-dashboard',
  assetPrefix: '/docker-container-dashboard/'
}

module.exports = nextConfig
